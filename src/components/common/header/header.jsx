import MenuBar from "./menu-bar/menu-bar";
import TopBar from "./top-bar/top-bar";

const Header = () => {
  return (
    <div>
      <TopBar />
      <MenuBar />
    </div>
  );
};

export default Header;
